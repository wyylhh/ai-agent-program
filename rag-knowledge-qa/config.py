"""
全局配置文件
修改此文件来切换模型、调整参数
"""
import os
from dotenv import load_dotenv

load_dotenv()

# ============================================================
# LLM 配置（兼容任何 OpenAI 兼容 API）
# ============================================================
LLM_API_BASE = os.getenv("LLM_API_BASE", "https://api.deepseek.com/v1")
LLM_API_KEY = os.getenv("LLM_API_KEY", "sk-e6ec7a09847d4ec8b4bf473ab2ab3e99")
LLM_MODEL = os.getenv("LLM_MODEL", "deepseek-chat")

# ============================================================
# Embedding 模型配置
# ============================================================
# 后端选择: "sklearn" = 离线 TF-IDF（无需下载，立即可用）
#          "bge"     = BGE 模型（需联网下载，质量更高）
EMBED_BACKEND = os.getenv("EMBED_BACKEND", "sklearn")
# BGE 模型名（仅 EMBED_BACKEND="bge" 时生效）
EMBED_MODEL_NAME = os.getenv("EMBED_MODEL", "BAAI/bge-small-zh-v1.5")

# ============================================================
# 文本分块参数
# ============================================================
CHUNK_SIZE = 512        # 每块最大字符数
CHUNK_OVERLAP = 50      # 相邻块重叠字符数

# ============================================================
# 向量数据库配置
# ============================================================
CHROMA_PERSIST_DIR = "./chroma_db"

# ============================================================
# 检索与生成参数
# ============================================================
TOP_K = 4               # 检索返回的最相关文档数
MAX_HISTORY = 6         # 保留最近 N 轮对话
TEMPERATURE = 0.3       # 生成温度，越低越确定
